<h2><%= @task.title %></h2>
<h2><%= @task.details %></h2>
<h2><%= @task.completed ? 'Completed, dog' : 'Do it already' %></h2>
<%= link_to 'Back to list', tasks_path%>
<%= link_to 'update task', edit_path %>
<%= link_to "Delete", destroy_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>

